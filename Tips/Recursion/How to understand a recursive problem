Ex: fib(n) {
      if(n == 0) return 0; 
      if(n == 1) return 1;
      return fib(n-1) + fib(n-2);
    }
    
    Take an example like f(4) and draw a tree for recursion. 
    
Ex: ReversePrintList(Node head) {
      if(head == null) return;
      ReversePrintList(head.next);
      print(head.val);
    }
    
    Take an example like 1 -> 2 -> 3 -> 4 and recurse.
